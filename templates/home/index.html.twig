
<!DOCTYPE html>
<html lang="en">

{% include 'partials/header.html.twig' %}
<body>
<section class="hero-section custom-nav ">
    <div class="container text-center">
        <h1 class="text-4xl font-bold mb-4">Discover Amazing Movies</h1>
        <p class="text-lg mb-8">Stream your favorite content anytime, anywhere</p>
        <div class="d-flex gap-4 justify-content-center">
            <a href="#" class="btn btn-danger px-5 py-3 rounded-pill">
                Start Watching
            </a>
            <a href="#" class="btn btn-dark px-5 py-3 rounded-pill">
                Browse Catalog
            </a>
        </div>
    </div>
</section>
{# <section class="movies-carousel my-5">
    <div class="container">
        <h2 class="text-center mb-4">Featured Content</h2>
        <div id="contentCarousel" class="carousel slide" data-bs-ride="carousel" data-bs-interval="5000">
            <div class="carousel-inner">
                {% for i in range(0, items|length, 4) %}
                    <div class="carousel-item {% if loop.first %}active{% endif %}">
                        <div class="row">
                            {% for item in items|slice(i, 4) %}
                                <div class="col-md-3">
                                    <div class="card">
                                        <img src="{{ item.image }}" class="card-img-top" alt="{{ item.title }}">
                                        <div class="card-body">
                                            <h5 class="card-title">{{ item.title }}</h5>
                                            <p class="card-text">{{ item.genre }}</p>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</section>

<section class="classics-section bg-light py-5">
    <div class="container">
        <h2 class="text-center mb-4">Monthly Classics</h2>
        <div class="row mb-4">
            <div class="col-12">
                <p class="text-center">
                    Each month, we carefully select three timeless classic films for our collectors. 
                    These masterpieces are available for purchase in limited quantities, featuring 
                    restored versions with exclusive bonus content.
                </p>
            </div>
        </div>
        <div class="row">
            {% for classic in classics %}
                <div class="col-md-4">
                    <div class="card h-100">
                        <img src="{{ classic.image }}" class="card-img-top" alt="{{ classic.title }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ classic.title }}</h5>
                            <p class="card-text">{{ classic.year }}</p>
                            <a href="#" class="btn btn-primary">Buy Now</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</section> #}
{% include 'partials/footer.html.twig' %}
</body>
</html>
