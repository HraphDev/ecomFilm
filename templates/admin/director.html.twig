{% extends 'admin/base.html.twig' %}

{% block title %}Directors{% endblock %}

{% block content %}
<div class="row mb-4">
    <div class="col-md-4 offset-md-4">
        <div class="card shadow-sm">
            <div class="card-body text-center">
                <h5 class="card-title">Manage Directors</h5>
                <a href="{{ path('director_create') }}" class="btn btn-primary">Add New Director</a>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-header text-center">
        <h1 class="h4">Director List</h1>
    </div>
    <div class="card-body">
        <table class="table table-bordered table-hover">
            <thead >
                <tr>
                    <th scope="col">Director Name</th>
                    <th scope="col" class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for director in directors %}
                    <tr>
                        <td>{{ director.name }}</td>
                        <td class="text-center">
                            <a href="{{ path('director_edit', { 'id': director.id }) }}" class="btn btn-warning btn-sm">
                                Edit
                            </a>
                            <a href="{{ path('director_delete', { 'id': director.id }) }}" 
                               class="btn btn-danger btn-sm" 
                               onclick="return confirm('Are you sure you want to delete this director?');">
                                Delete
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2" class="text-center text-muted">No directors available</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
