<!DOCTYPE html>
<html lang="en">

{% include 'partials/header.html.twig' %}
<br>
<div class="container py-5">
    <div class="row">
        <div class="col-md-8">
            <h1 class="display-4">{{ entity.title }}</h1>
            <p class="text-muted">Release Date: {{ entity.releaseDate|date('Y-m-d') }}</p>
            <p>{{ entity.description }}</p>

            <div class="embed-responsive embed-responsive-16by9">
                <video class="embed-responsive-item" controls>
                    <source src="{{ asset(entity.videoPath) }}" type="video/mp4">
                    Your browser does not support the video tag.
                </video>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <img src="{{ asset(entity.imagePath) }}" alt="{{ entity.title }}" class="img-fluid mt-8">
                    <ul class="list-group">
                        {% for category in classicalFilmsCategories %}
                            <li class="list-group-item">Category: {{ category.name }}</li>
                        {% endfor %}
                    </ul>
                    <br>
                    <p class="card-text">{{ entity.description }}</p>
                    <p class="font-weight-bold">Market Price: ${{ entity.price }}</p>
                </div>
            </div>
        </div>
    </div>
</div>

{% include 'partials/footer.html.twig' %}
</html>
